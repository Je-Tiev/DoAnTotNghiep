% preamble.tex — Báo cáo tốt nghiệp, hỗ trợ tiếng Việt.
% Biên dịch bằng XeLaTeX hoặc LuaLaTeX (KHÔNG dùng pdflatex):
%   latexmk -xelatex main.tex
% Nạp trong main.tex:  \input{preamble}

\documentclass[a4paper,13pt,oneside]{report}

% --- Font & tiếng Việt (XeLaTeX/LuaLaTeX) ---------------------------------
\usepackage{fontspec}
\setmainfont{Times New Roman}          % hoặc font có dấu tiếng Việt đầy đủ
\usepackage{polyglossia}
\setdefaultlanguage{vietnamese}
\setotherlanguage{english}

% --- Bố cục trang ---------------------------------------------------------
\usepackage[top=2cm,bottom=2cm,left=3cm,right=2cm]{geometry}
\usepackage{setspace}
\onehalfspacing
\usepackage{indentfirst}
\setlength{\parindent}{1cm}

% --- Hình, bảng, đồ thị ---------------------------------------------------
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}

% --- Code / listing -------------------------------------------------------
\usepackage{listings}
\usepackage{xcolor}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{teal},
  numbers=left, numberstyle=\tiny, numbersep=6pt,
  frame=single, breaklines=true, captionpos=b,
  showstringspaces=false, tabsize=2,
}
\lstdefinestyle{c}{language=C}

% --- Tham chiếu chéo & link ----------------------------------------------
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=black, citecolor=black, urlcolor=blue}
\usepackage{cleveref}

% --- Tài liệu tham khảo ---------------------------------------------------
% \usepackage[backend=biber,style=ieee]{biblatex}
% \addbibresource{refs.bib}
